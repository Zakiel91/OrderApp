---
phase: 02-mobile-ux-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/context/WizardNavContext.tsx
  - src/context/OrderFormContext.tsx
  - src/context/FixFormContext.tsx
  - src/steps/Step6Review.tsx
  - src/fix-steps/FixStep3Review.tsx
  - src/App.tsx
autonomous: true
requirements:
  - UX-06

must_haves:
  truths:
    - "WizardNavContext существует и экспортирует хук useWizardNav"
    - "OrderFormContext передаёт step, totalSteps, setStep, submitHandler, registerSubmitHandler через WizardNavContext"
    - "FixFormContext передаёт step, totalSteps, setStep, submitHandler, registerSubmitHandler через WizardNavContext"
    - "Step6Review регистрирует handleSubmit в OrderFormContext при монтировании и очищает при размонтировании"
    - "FixStep3Review регистрирует handleSubmit в FixFormContext при монтировании и очищает при размонтировании"
    - "WizardNavProvider обёртывает AppRoutes — BottomNav может читать из него без ошибок"
  artifacts:
    - path: "src/context/WizardNavContext.tsx"
      provides: "WizardNavProvider, useWizardNav, WizardNavContextType"
      exports: ["WizardNavProvider", "useWizardNav"]
    - path: "src/context/OrderFormContext.tsx"
      provides: "Расширенный интерфейс OrderFormContextType с submitHandler и registerSubmitHandler"
      contains: "registerSubmitHandler"
    - path: "src/context/FixFormContext.tsx"
      provides: "Расширенный интерфейс FixFormContextType с submitHandler и registerSubmitHandler"
      contains: "registerSubmitHandler"
    - path: "src/steps/Step6Review.tsx"
      provides: "handleSubmit обёрнут в useCallback, зарегистрирован через registerSubmitHandler"
      contains: "registerSubmitHandler"
    - path: "src/fix-steps/FixStep3Review.tsx"
      provides: "handleSubmit обёрнут в useCallback, зарегистрирован через registerSubmitHandler"
      contains: "registerSubmitHandler"
  key_links:
    - from: "src/context/OrderFormContext.tsx"
      to: "src/context/WizardNavContext.tsx"
      via: "useWizardNav() + useEffect синхронизация step/submit"
      pattern: "useWizardNav"
    - from: "src/steps/Step6Review.tsx"
      to: "src/context/OrderFormContext.tsx"
      via: "registerSubmitHandler(handleSubmit)"
      pattern: "registerSubmitHandler"
    - from: "src/App.tsx"
      to: "src/context/WizardNavContext.tsx"
      via: "WizardNavProvider обёртывает AppRoutes"
      pattern: "WizardNavProvider"
---

<objective>
Создать WizardNavContext как лёгкий мост между wizard-провайдерами (OrderFormContext, FixFormContext) и BottomNav.

Purpose: BottomNav находится вне OrderFormProvider и FixFormProvider в дереве компонентов (App.tsx). Прямой вызов useOrderForm() из BottomNav вызовет runtime error. WizardNavContext — отдельный провайдер, поднятый выше BottomNav, который визарды заполняют сами.

Output:
- src/context/WizardNavContext.tsx — новый файл
- src/context/OrderFormContext.tsx — расширен registerSubmitHandler + submitHandler
- src/context/FixFormContext.tsx — расширен аналогично
- src/steps/Step6Review.tsx — handleSubmit → useCallback + регистрация
- src/fix-steps/FixStep3Review.tsx — то же
- src/App.tsx — WizardNavProvider добавлен выше BottomNav
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/OrderApp/.planning/PROJECT.md
@C:/OrderApp/.planning/ROADMAP.md
@C:/OrderApp/.planning/STATE.md
@C:/OrderApp/.planning/phases/02-mobile-ux-foundation/02-CONTEXT.md
@C:/OrderApp/.planning/phases/02-mobile-ux-foundation/02-RESEARCH.md
</context>

<interfaces>
<!-- Текущие интерфейсы — прочитаны из кодовой базы. Executor должен использовать их напрямую. -->

Из src/context/OrderFormContext.tsx (текущий интерфейс, lines 4-12):
```typescript
interface OrderFormContextType {
  form: OrderFormData
  updateField: <K extends keyof OrderFormData>(key: K, value: OrderFormData[K]) => void
  updateFields: (updates: Partial<OrderFormData>) => void
  resetForm: () => void
  step: number
  setStep: (s: number) => void
  totalSteps: number
}
```

Из src/context/FixFormContext.tsx (текущий интерфейс, lines 4-12):
```typescript
interface FixFormContextType {
  form: FixFormData
  updateField: <K extends keyof FixFormData>(key: K, value: FixFormData[K]) => void
  updateFields: (updates: Partial<FixFormData>) => void
  resetForm: () => void
  step: number
  setStep: (s: number) => void
  totalSteps: number
}
```

Из src/App.tsx (tree structure, lines 26-47):
```tsx
// BottomNav — сосед Routes, НЕ потомок OrderFormProvider/FixFormProvider
<>
  <div className="max-w-lg mx-auto min-h-screen pb-[68px]">
    <Routes>
      <Route path="/orders/new" element={
        <OrderFormProvider><NewOrderPage /></OrderFormProvider>
      } />
      <Route path="/orders/fix" element={
        <FixFormProvider><FixOrderPage /></FixFormProvider>
      } />
      ...
    </Routes>
  </div>
  <BottomNav />
</>

Из src/components/FormField.tsx (line 30-32):
```typescript
export const buttonClass = 'w-full bg-[var(--color-primary)] text-white font-semibold rounded-xl px-4 py-4 min-h-[52px] active:bg-[var(--color-primary-dark)] transition-all tracking-wider uppercase text-[15px] disabled:opacity-40 disabled:cursor-not-allowed shadow-md hover:shadow-lg'
export const secondaryButtonClass = 'w-full bg-[var(--color-surface)] text-[var(--color-text)] font-medium rounded-xl px-4 py-3.5 min-h-[50px] active:bg-[var(--color-surface-light)] transition-colors border border-[var(--color-border)] text-[15px] shadow-sm'
```
// ВАЖНО: buttonClass содержит w-full — в flex-контейнере BottomNav нужно !w-auto flex-1
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Создать WizardNavContext + расширить OrderFormContext и FixFormContext</name>
  <files>
    src/context/WizardNavContext.tsx,
    src/context/OrderFormContext.tsx,
    src/context/FixFormContext.tsx
  </files>
  <read_first>
    - src/context/OrderFormContext.tsx — текущий интерфейс и паттерн useState+useCallback+Provider
    - src/context/FixFormContext.tsx — зеркальная структура
    - src/App.tsx — позиция BottomNav в дереве (вне Provider-ов)
  </read_first>
  <action>
**1. Создать src/context/WizardNavContext.tsx (новый файл):**

```typescript
import { createContext, useContext, useState, useCallback, type ReactNode } from 'react'

export interface WizardNavState {
  step: number
  totalSteps: number
  setStep: (s: number) => void
  submitHandler: (() => Promise<void>) | null
  submitting: boolean
}

interface WizardNavContextType {
  state: WizardNavState | null
  setWizardState: (state: WizardNavState | null) => void
}

const WizardNavContext = createContext<WizardNavContextType | null>(null)

export function WizardNavProvider({ children }: { children: ReactNode }) {
  const [state, setStateInternal] = useState<WizardNavState | null>(null)
  const setWizardState = useCallback((s: WizardNavState | null) => {
    setStateInternal(s)
  }, [])
  return (
    <WizardNavContext.Provider value={{ state, setWizardState }}>
      {children}
    </WizardNavContext.Provider>
  )
}

export function useWizardNav() {
  const ctx = useContext(WizardNavContext)
  if (!ctx) throw new Error('useWizardNav must be used within WizardNavProvider')
  return ctx
}
```

**2. Расширить src/context/OrderFormContext.tsx:**

Добавить в интерфейс `OrderFormContextType` два новых поля (после `totalSteps`):
```typescript
  submitHandler: (() => Promise<void>) | null
  registerSubmitHandler: (fn: (() => Promise<void>) | null) => void
```

Добавить в `OrderFormProvider` новый state и callback (после строки с `totalSteps = 5`):
```typescript
  const [submitHandler, setSubmitHandler] = useState<(() => Promise<void>) | null>(null)
  const registerSubmitHandler = useCallback((fn: (() => Promise<void>) | null) => {
    setSubmitHandler(() => fn)  // стрелочная функция ОБЯЗАТЕЛЬНА — иначе React вызовет fn как updater
  }, [])
```

Добавить import useWizardNav и useEffect синхронизации с WizardNavContext в OrderFormProvider (после registerSubmitHandler):
```typescript
  const { setWizardState } = useWizardNav()
  useEffect(() => {
    setWizardState({ step, totalSteps, setStep, submitHandler, submitting: false })
    return () => setWizardState(null)
  }, [step, totalSteps, setStep, submitHandler, setWizardState])
```

Добавить импорт: `import { useWizardNav } from './WizardNavContext'`
Добавить `useEffect` в импорт из 'react' (уже есть — проверить).

Расширить Provider value:
```typescript
<OrderFormContext.Provider value={{
  form, updateField, updateFields, resetForm, step, setStep, totalSteps,
  submitHandler, registerSubmitHandler,
}}>
```

**3. Расширить src/context/FixFormContext.tsx — идентично OrderFormContext:**

Те же четыре изменения:
- Добавить `submitHandler` и `registerSubmitHandler` в `FixFormContextType`
- Добавить `useState` + `useCallback` в `FixFormProvider`
- Добавить `useWizardNav` + `useEffect` синхронизации (те же зависимости)
- Расширить `FixFormContext.Provider value`

Примечание: submitting не поднимается в WizardNavContext в этом плане — BottomNav будет управлять своим локальным состоянием loading при вызове submitHandler через Promise (Plan 02).
  </action>
  <verify>
    <automated>cd C:/OrderApp && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/context/WizardNavContext.tsx существует: `grep -n "WizardNavProvider\|useWizardNav\|WizardNavState" C:/OrderApp/src/context/WizardNavContext.tsx` возвращает минимум 3 строки
    - OrderFormContext расширен: `grep -n "registerSubmitHandler\|submitHandler" C:/OrderApp/src/context/OrderFormContext.tsx` возвращает минимум 4 строки
    - FixFormContext расширен: `grep -n "registerSubmitHandler\|submitHandler" C:/OrderApp/src/context/FixFormContext.tsx` возвращает минимум 4 строки
    - Синхронизация: `grep -n "setWizardState" C:/OrderApp/src/context/OrderFormContext.tsx` и `C:/OrderApp/src/context/FixFormContext.tsx` — по 1+ строке в каждом
    - npm run build завершается без TypeScript ошибок
  </acceptance_criteria>
  <done>WizardNavContext создан с Provider и хуком. OrderFormContext и FixFormContext расширены полями submitHandler/registerSubmitHandler и синхронизируются с WizardNavContext через useEffect. Build проходит.</done>
</task>

<task type="auto">
  <name>Task 2: Зарегистрировать handleSubmit в Step6Review и FixStep3Review + подключить WizardNavProvider в App.tsx</name>
  <files>
    src/steps/Step6Review.tsx,
    src/fix-steps/FixStep3Review.tsx,
    src/App.tsx
  </files>
  <read_first>
    - src/steps/Step6Review.tsx — текущий handleSubmit (lines 35-122), кнопка Submit (lines 205-211)
    - src/fix-steps/FixStep3Review.tsx — текущий handleSubmit (lines 36-95), кнопка Submit (line 148)
    - src/App.tsx — импорты и структура AppRoutes
    - src/context/WizardNavContext.tsx — только что созданный файл (Task 1)
  </read_first>
  <action>
**1. Изменить src/steps/Step6Review.tsx:**

Добавить `useCallback` в импорт из 'react' (line 1: `import { useState, useCallback, useEffect } from 'react'`).

Добавить импорт: `import { useOrderForm } from '../context/OrderFormContext'` — уже есть.

Обернуть `handleSubmit` в `useCallback`. Текущие зависимости handleSubmit: `form`, `t`, `resetForm`. Заменить `const handleSubmit = async () => {` на:
```typescript
const handleSubmit = useCallback(async () => {
  // ... всё тело handleSubmit без изменений ...
}, [form, t, resetForm])
```

Добавить `registerSubmitHandler` из `useOrderForm()` в деструктуризацию (line 28):
```typescript
const { form, resetForm, registerSubmitHandler } = useOrderForm()
```

Добавить `useEffect` регистрации ПОСЛЕ объявления `handleSubmit`:
```typescript
useEffect(() => {
  registerSubmitHandler(handleSubmit)
  return () => registerSubmitHandler(null)
}, [handleSubmit, registerSubmitHandler])
```

Удалить кнопку Submit из JSX (lines 205-211 в оригинале):
```tsx
// УДАЛИТЬ ЭТИ СТРОКИ:
<button
  className={buttonClass}
  onClick={handleSubmit}
  disabled={submitting}
>
  {submitting ? t('loading') : t('submit_order')}
</button>
```
Кнопку `buttonClass` для `resetForm` (в `submitted` блоке) — ОСТАВИТЬ без изменений.
Удалить импорт `buttonClass` из FormField ТОЛЬКО если он больше не используется в файле (в submitted-блоке `buttonClass` используется — НЕ удалять).

**2. Изменить src/fix-steps/FixStep3Review.tsx — аналогично:**

Добавить `useCallback`, `useEffect` в импорт из 'react'.
Добавить `registerSubmitHandler` в деструктуризацию из `useFixForm()`.
Обернуть `handleSubmit` в `useCallback` (зависимости: `form`, `t`, `resetForm`).
Добавить `useEffect` регистрации после handleSubmit.
Удалить кнопку Submit (line 148):
```tsx
// УДАЛИТЬ:
<button className={buttonClass} onClick={handleSubmit} disabled={submitting}>
  {submitting ? t('submitting') : t('fix_submit')}
</button>
```
Импорт `buttonClass` в FixStep3Review.tsx — проверить, используется ли ещё в success-блоке (line 108 — ДА, используется) — НЕ удалять.

**3. Изменить src/App.tsx:**

Добавить импорт:
```typescript
import { WizardNavProvider } from './context/WizardNavContext'
```

Обернуть содержимое функции `AppRoutes` (весь `<>...</>` блок, lines 26-47) в `<WizardNavProvider>`:
```tsx
return (
  <WizardNavProvider>
    <>
      <div className="max-w-lg mx-auto min-h-screen pb-[68px]">
        <Routes>
          ...
        </Routes>
      </div>
      <BottomNav />
    </>
  </WizardNavProvider>
)
```
Блок `if (!user)` — НЕ оборачивать.
  </action>
  <verify>
    <automated>cd C:/OrderApp && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Step6Review регистрирует submit: `grep -n "registerSubmitHandler" C:/OrderApp/src/steps/Step6Review.tsx` возвращает 2+ строки (в useEffect и в деструктуризации)
    - Кнопка Submit удалена из Step6Review: `grep -n "onClick={handleSubmit}" C:/OrderApp/src/steps/Step6Review.tsx` возвращает 0 строк
    - FixStep3Review регистрирует submit: `grep -n "registerSubmitHandler" C:/OrderApp/src/fix-steps/FixStep3Review.tsx` возвращает 2+ строки
    - Кнопка Submit удалена из FixStep3Review: `grep -n "onClick={handleSubmit}" C:/OrderApp/src/fix-steps/FixStep3Review.tsx` возвращает 0 строк
    - WizardNavProvider в App.tsx: `grep -n "WizardNavProvider" C:/OrderApp/src/App.tsx` возвращает 3 строки (импорт + открывающий тег + закрывающий тег)
    - npm run build завершается без TypeScript ошибок
  </acceptance_criteria>
  <done>Step6Review и FixStep3Review регистрируют handleSubmit через useCallback + useEffect. Кнопки Submit удалены из обоих компонентов. WizardNavProvider оборачивает AppRoutes в App.tsx. Build проходит чисто.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Step6Review → OrderFormContext | handleSubmit перемещается из локального state в контекст |
| FixStep3Review → FixFormContext | то же для Fix wizard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | registerSubmitHandler / WizardNavContext | accept | Контекст доступен только внутри React-дерева приложения. Нет внешних вызовов. Тот же API-вызов, тот же JWT, тот же payload — атаковать нечего. |
| T-02-02 | Spoofing | handleSubmit в контексте | accept | handleSubmit регистрируется только из Step6Review/FixStep3Review при монтировании. Подменить его снаружи компонента невозможно без доступа к React tree. |
| T-02-03 | Information Disclosure | WizardNavState | accept | WizardNavState содержит только step (число), totalSteps (число), setStep (функция), submitHandler (функция). Никаких персональных данных или токенов. |

**Оценка уровня угрозы: LOW** — фаза не вводит новых API-эндпоинтов, не меняет аутентификацию, не добавляет хранения данных. Submit-обработчик остаётся тем же; меняется только место его вызова (из BottomNav вместо кнопки в степе). Существующие защиты (JWT в заголовке, серверный фильтр по salesman_id) не затрагиваются.
</threat_model>

<verification>
После выполнения обоих задач:

1. `npm run build` — без ошибок TypeScript
2. `npm run lint` — без предупреждений (если lint настроен)
3. Ручная проверка: открыть `/orders/new` — runtime error "useOrderForm must be used within OrderFormProvider" НЕ должна появляться
4. Ручная проверка: Step6Review рендерится без кнопки Submit (она появится в BottomNav после Plan 02)
</verification>

<success_criteria>
- WizardNavContext создан и провайдер подключён выше BottomNav
- OrderFormContext и FixFormContext синхронизируют step/submitHandler с WizardNavContext
- Step6Review и FixStep3Review регистрируют handleSubmit через registerSubmitHandler с cleanup
- Кнопки Submit удалены из шагов-ревью (переместятся в BottomNav в Plan 02)
- TypeScript build проходит чисто
</success_criteria>

<output>
После завершения создать C:/OrderApp/.planning/phases/02-mobile-ux-foundation/02-01-SUMMARY.md
</output>
