---
phase: 02-mobile-ux-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01-PLAN.md
files_modified:
  - src/components/BottomNav.tsx
  - src/pages/NewOrderPage.tsx
  - src/pages/FixOrderPage.tsx
autonomous: true
requirements:
  - UX-01
  - UX-06

must_haves:
  truths:
    - "BottomNav показывает кнопки Back/Next вместо вкладок на /orders/new и /orders/fix"
    - "На первом шаге показывается только Next (нет Back)"
    - "На последнем шаге Next заменяется на Submit"
    - "Submit вызывает зарегистрированный обработчик из WizardNavContext"
    - "При смене шага страница мгновенно скроллится в начало (без анимации)"
    - "Inline Back/Next кнопки удалены из NewOrderPage и FixOrderPage"
  artifacts:
    - path: "src/components/BottomNav.tsx"
      provides: "Wizard mode: Back/Next/Submit через WizardNavContext"
      contains: "isWizard"
    - path: "src/pages/NewOrderPage.tsx"
      provides: "useEffect scroll reset + удалены inline-кнопки"
      contains: "window.scrollTo"
    - path: "src/pages/FixOrderPage.tsx"
      provides: "useEffect scroll reset + удалены inline-кнопки"
      contains: "window.scrollTo"
  key_links:
    - from: "src/components/BottomNav.tsx"
      to: "src/context/WizardNavContext.tsx"
      via: "useWizardNav() — читает state.step, state.totalSteps, state.setStep, state.submitHandler"
      pattern: "useWizardNav"
    - from: "src/pages/NewOrderPage.tsx"
      to: "window"
      via: "useEffect([step]) → window.scrollTo(0, 0)"
      pattern: "window.scrollTo"
---

<objective>
Подключить BottomNav к WizardNavContext для отображения Back/Next/Submit в wizard mode. Добавить scroll reset в оба page-компонента. Удалить inline-кнопки Back/Next.

Purpose: UX-06 — кнопки навигации должны быть всегда видны (fixed bottom). UX-01 — при смене шага страница должна начинаться сверху.

Output:
- BottomNav.tsx — wizard mode с Back/Next/Submit
- NewOrderPage.tsx — удалены inline кнопки, добавлен scroll reset
- FixOrderPage.tsx — то же
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/OrderApp/.planning/ROADMAP.md
@C:/OrderApp/.planning/STATE.md
@C:/OrderApp/.planning/phases/02-mobile-ux-foundation/02-CONTEXT.md
@C:/OrderApp/.planning/phases/02-mobile-ux-foundation/02-RESEARCH.md
@C:/OrderApp/.planning/phases/02-mobile-ux-foundation/02-UI-SPEC.md
@C:/OrderApp/.planning/phases/02-mobile-ux-foundation/02-01-SUMMARY.md
</context>

<interfaces>
<!-- Контракты из Plan 01 и существующего кода. -->

Из src/context/WizardNavContext.tsx (создан в Plan 01):
```typescript
export interface WizardNavState {
  step: number
  totalSteps: number
  setStep: (s: number) => void
  submitHandler: (() => Promise<void>) | null
  submitting: boolean
}

export function useWizardNav(): {
  state: WizardNavState | null
  setWizardState: (state: WizardNavState | null) => void
}
```

Из src/components/FormField.tsx:
```typescript
// buttonClass содержит w-full — в flex-контейнере нужно !w-auto flex-1
export const buttonClass = 'w-full bg-[var(--color-primary)] text-white font-semibold rounded-xl px-4 py-4 min-h-[52px] active:bg-[var(--color-primary-dark)] transition-all tracking-wider uppercase text-[15px] disabled:opacity-40 disabled:cursor-not-allowed shadow-md hover:shadow-lg'
export const secondaryButtonClass = 'w-full bg-[var(--color-surface)] text-[var(--color-text)] font-medium rounded-xl px-4 py-3.5 min-h-[50px] active:bg-[var(--color-surface-light)] transition-colors border border-[var(--color-border)] text-[15px] shadow-sm'
```

Из src/components/BottomNav.tsx (текущий файл — весь):
```typescript
import { useLocation, useNavigate } from 'react-router'
import { useLanguage } from '../context/LanguageContext'
// Нужно добавить: import { useWizardNav } from '../context/WizardNavContext'
//               import { buttonClass, secondaryButtonClass } from './FormField'
//               import { useState } from 'react'

const tabs = [
  { path: '/orders', labelKey: 'nav_orders', icon: '📋' },
  { path: '/orders/new', labelKey: 'nav_new', icon: '➕' },
  { path: '/orders/fix', labelKey: 'nav_fix', icon: '🔧' },
  { path: '/settings', labelKey: 'nav_settings', icon: '⚙️' },
]
// Wizard mode detection по pathname (тот же паттерн что active tab):
// const isWizard = location.pathname === '/orders/new' || location.pathname === '/orders/fix'
```

Из src/pages/NewOrderPage.tsx (текущий, inline-кнопки — lines 43-61):
```tsx
// БЛОК ДЛЯ УДАЛЕНИЯ (lines 43-61):
{step < totalSteps && (
  <div className="p-4 flex gap-3">
    {step > 1 && (
      <button className={secondaryButtonClass} onClick={() => setStep(step - 1)}>
        {t('back')}
      </button>
    )}
    <button className={buttonClass} onClick={() => setStep(step + 1)}>
      {t('next')}
    </button>
  </div>
)}
{step === totalSteps && step > 1 && (
  <div className="px-4">
    <button className={secondaryButtonClass} onClick={() => setStep(step - 1)}>
      {t('back')}
    </button>
  </div>
)}
// После удаления: импорт buttonClass, secondaryButtonClass из FormField больше не нужен
```

Из UI-SPEC (BottomNav wizard mode layout):
```
- nav: fixed bottom-0 left-0 right-0, bg-[var(--color-surface)], border-t border-[var(--color-border)], safe-area-bottom, z-50
- inner div: flex gap-2 px-4 py-2, max-w-lg mx-auto
- Back button: secondaryButtonClass + ' !w-auto flex-1' (hidden on step 1)
- Next/Submit button: buttonClass + ' !w-auto flex-1'
- Submit disabled style: уже в buttonClass через disabled:opacity-40
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: BottomNav wizard mode — Back/Next/Submit через WizardNavContext</name>
  <files>src/components/BottomNav.tsx</files>
  <read_first>
    - src/components/BottomNav.tsx — полный текущий файл (38 строк)
    - src/context/WizardNavContext.tsx — WizardNavState интерфейс и useWizardNav хук (создан в Plan 01)
    - src/components/FormField.tsx — buttonClass и secondaryButtonClass (строки 30-32)
  </read_first>
  <action>
Переписать src/components/BottomNav.tsx. Сохранить весь стандартный режим без изменений. Добавить wizard mode.

**Итоговый файл:**

```typescript
import { useState } from 'react'
import { useLocation, useNavigate } from 'react-router'
import { useLanguage } from '../context/LanguageContext'
import { useWizardNav } from '../context/WizardNavContext'
import { buttonClass, secondaryButtonClass } from './FormField'

const tabs = [
  { path: '/orders', labelKey: 'nav_orders', icon: '📋' },
  { path: '/orders/new', labelKey: 'nav_new', icon: '➕' },
  { path: '/orders/fix', labelKey: 'nav_fix', icon: '🔧' },
  { path: '/settings', labelKey: 'nav_settings', icon: '⚙️' },
]

export function BottomNav() {
  const location = useLocation()
  const navigate = useNavigate()
  const { t } = useLanguage()
  const { state } = useWizardNav()
  const [submitting, setSubmitting] = useState(false)

  const isWizard = location.pathname === '/orders/new' || location.pathname === '/orders/fix'

  // Wizard mode
  if (isWizard && state) {
    const { step, totalSteps, setStep, submitHandler } = state
    const isLastStep = step === totalSteps

    const handleSubmit = async () => {
      if (!submitHandler) return
      setSubmitting(true)
      try {
        await submitHandler()
      } finally {
        setSubmitting(false)
      }
    }

    return (
      <nav
        className="fixed bottom-0 left-0 right-0 bg-[var(--color-surface)] border-t border-[var(--color-border)] safe-area-bottom z-50"
        style={{ boxShadow: '0 -4px 16px rgba(0,0,0,0.06)' }}
      >
        <div className="flex gap-2 px-4 py-2 max-w-lg mx-auto">
          {step > 1 && (
            <button
              className={secondaryButtonClass + ' !w-auto flex-1'}
              onClick={() => setStep(step - 1)}
              disabled={submitting}
            >
              {t('back')}
            </button>
          )}
          {isLastStep ? (
            <button
              className={buttonClass + ' !w-auto flex-1'}
              onClick={handleSubmit}
              disabled={submitting || !submitHandler}
            >
              {submitting ? t('loading') : `✓ ${t('submit_order')}`}
            </button>
          ) : (
            <button
              className={buttonClass + ' !w-auto flex-1'}
              onClick={() => setStep(step + 1)}
            >
              {t('next')}
            </button>
          )}
        </div>
      </nav>
    )
  }

  // Standard tab mode (unchanged)
  return (
    <nav
      className="fixed bottom-0 left-0 right-0 bg-[var(--color-surface)] border-t border-[var(--color-border)] safe-area-bottom z-50"
      style={{ boxShadow: '0 -4px 16px rgba(0,0,0,0.06)' }}
    >
      <div className="flex justify-around max-w-lg mx-auto">
        {tabs.map(tab => {
          const active = location.pathname === tab.path ||
            (tab.path === '/orders' && location.pathname.startsWith('/orders/') && !location.pathname.includes('new') && !location.pathname.includes('fix'))
          return (
            <button
              key={tab.path}
              onClick={() => navigate(tab.path)}
              className={`flex flex-col items-center py-2 px-3 min-h-[52px] flex-1 transition-colors ${
                active ? 'text-[var(--color-accent)]' : 'text-[var(--color-text-muted)]'
              }`}
            >
              <span className="text-xl">{tab.icon}</span>
              <span className="text-[11px] mt-0.5">{t(tab.labelKey)}</span>
            </button>
          )
        })}
      </div>
    </nav>
  )
}
```

Ключевые моменты:
- `!w-auto flex-1` на wizard-кнопках переопределяет `w-full` из buttonClass/secondaryButtonClass
- `state` может быть null в момент до монтирования визарда — условие `isWizard && state` защищает от ошибки
- `submitting` — локальный state BottomNav, управляется через Promise.finally
- Back disabled при submitting — не даём уйти во время отправки
- Submit disabled если submitHandler === null (степ ещё не смонтирован)
  </action>
  <verify>
    <automated>cd C:/OrderApp && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Wizard mode присутствует: `grep -n "isWizard" C:/OrderApp/src/components/BottomNav.tsx` возвращает 2+ строки
    - useWizardNav импортирован: `grep -n "useWizardNav" C:/OrderApp/src/components/BottomNav.tsx` возвращает 2 строки (импорт + вызов)
    - buttonClass импортирован: `grep -n "buttonClass\|secondaryButtonClass" C:/OrderApp/src/components/BottomNav.tsx` возвращает 3+ строки
    - `!w-auto flex-1` присутствует: `grep -n "w-auto flex-1" C:/OrderApp/src/components/BottomNav.tsx` возвращает 2 строки
    - submit_order i18n ключ: `grep -n "submit_order" C:/OrderApp/src/components/BottomNav.tsx` возвращает 1 строку
    - npm run build без TypeScript ошибок
  </acceptance_criteria>
  <done>BottomNav в wizard mode показывает Back (скрыт на step 1) / Next / Submit (на последнем шаге). Стандартный режим вкладок не изменён. Build проходит.</done>
</task>

<task type="auto">
  <name>Task 2: Scroll reset + удалить inline Back/Next из NewOrderPage и FixOrderPage</name>
  <files>
    src/pages/NewOrderPage.tsx,
    src/pages/FixOrderPage.tsx
  </files>
  <read_first>
    - src/pages/NewOrderPage.tsx — полный текущий файл (64 строки)
    - src/pages/FixOrderPage.tsx — полный текущий файл (52 строки)
  </read_first>
  <action>
**1. src/pages/NewOrderPage.tsx:**

Изменить импорт React (line 1) — добавить `useRef`:
```typescript
import { useEffect, useRef } from 'react'
```

Удалить импорт `buttonClass, secondaryButtonClass` из `'../components/FormField'` (line 6) — эти классы больше не нужны в этом файле.

Добавить scroll reset useEffect ПОСЛЕ существующего useEffect (после строки 36) и ПЕРЕД return:
```typescript
// UX-01: scroll to top on step change (per D-04, D-05, D-06)
const isFirstRender = useRef(true)
useEffect(() => {
  if (isFirstRender.current) { isFirstRender.current = false; return }
  window.scrollTo(0, 0)
}, [step])
```

Удалить оба блока inline-кнопок (per D-03):
```tsx
// УДАЛИТЬ (lines ~43-53):
{step < totalSteps && (
  <div className="p-4 flex gap-3">
    {step > 1 && (
      <button className={secondaryButtonClass} onClick={() => setStep(step - 1)}>
        {t('back')}
      </button>
    )}
    <button className={buttonClass} onClick={() => setStep(step + 1)}>
      {t('next')}
    </button>
  </div>
)}
// УДАЛИТЬ (lines ~55-61):
{step === totalSteps && step > 1 && (
  <div className="px-4">
    <button className={secondaryButtonClass} onClick={() => setStep(step - 1)}>
      {t('back')}
    </button>
  </div>
)}
```

Результирующий return должен содержать только:
```tsx
return (
  <div className="pb-28">
    <ProgressBar current={step} total={totalSteps} />
    <StepComponent />
  </div>
)
```

**2. src/pages/FixOrderPage.tsx — идентично:**

Изменить импорт (line 1): `import { useEffect, useRef } from 'react'`
Удалить импорт `buttonClass, secondaryButtonClass` (line 6).
Добавить scroll reset useEffect с `useRef` (после существующего useEffect, lines 19-24).
Удалить оба блока inline-кнопок (lines ~31-49).

Результирующий return:
```tsx
return (
  <div className="pb-28">
    <ProgressBar current={step} total={totalSteps} />
    <StepComponent />
  </div>
)
```
  </action>
  <verify>
    <automated>cd C:/OrderApp && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Scroll reset в NewOrderPage: `grep -n "window.scrollTo" C:/OrderApp/src/pages/NewOrderPage.tsx` возвращает 1 строку
    - useRef в NewOrderPage: `grep -n "useRef\|isFirstRender" C:/OrderApp/src/pages/NewOrderPage.tsx` возвращает 2+ строки
    - Inline кнопки удалены из NewOrderPage: `grep -n "secondaryButtonClass\|buttonClass" C:/OrderApp/src/pages/NewOrderPage.tsx` возвращает 0 строк
    - Scroll reset в FixOrderPage: `grep -n "window.scrollTo" C:/OrderApp/src/pages/FixOrderPage.tsx` возвращает 1 строку
    - Inline кнопки удалены из FixOrderPage: `grep -n "secondaryButtonClass\|buttonClass" C:/OrderApp/src/pages/FixOrderPage.tsx` возвращает 0 строк
    - npm run build без TypeScript ошибок
  </acceptance_criteria>
  <done>Оба page-компонента: inline Back/Next удалены, scroll reset через useRef+useEffect добавлен. Build проходит.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BottomNav → WizardNavContext → submitHandler | BottomNav вызывает submitHandler, зарегистрированный визардом |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | BottomNav → submitHandler() | accept | submitHandler — тот же handleSubmit что и раньше. Тот же API-вызов (createOrder), тот же JWT в заголовке, тот же payload. Перемещение точки вызова не создаёт новой поверхности атаки. |
| T-02-05 | Denial of Service | submitting state в BottomNav | accept | Кнопка Submit disabled пока submitting=true — предотвращает двойную отправку. Стандартная защита на UI-уровне. |
| T-02-06 | Elevation of Privilege | window.scrollTo | accept | window.scrollTo(0,0) — не имеет отношения к безопасности. Pure UI операция. |

**Оценка уровня угрозы: LOW** — перемещение вызова submitHandler из кнопки в шаге в кнопку в BottomNav не меняет логику API-запроса. Сервер по-прежнему проверяет JWT, фильтрует по salesman_id.
</threat_model>

<verification>
После выполнения обоих задач (и после Plan 01):

1. `npm run build` — без ошибок
2. Открыть `/orders/new` на мобильном (или DevTools mobile mode):
   - Должны исчезнуть inline-кнопки внутри контента
   - BottomNav должен показывать "Next" (шаг 1, нет Back)
   - При переходе на шаг 2 — появляется "Back", страница скроллится вверх
   - На последнем шаге — BottomNav показывает "Back" и "✓ Submit"
   - Нажатие Submit отправляет заказ, кнопки disabled во время отправки
3. Открыть `/orders/fix` — то же поведение (3 шага)
4. Перейти на `/orders` — BottomNav показывает стандартные вкладки
</verification>

<success_criteria>
- BottomNav в wizard mode: Back скрыт на step 1, показан на steps 2+; Next на середине; Submit на последнем шаге
- Submit в BottomNav вызывает тот же handleSubmit что и раньше (через WizardNavContext)
- Нажатие любой кнопки навигации (Back/Next) вызывает мгновенный scroll to top
- Inline кнопки удалены из обоих page-компонентов
- TypeScript build чист, нет регрессий в стандартном режиме вкладок
</success_criteria>

<output>
После завершения создать C:/OrderApp/.planning/phases/02-mobile-ux-foundation/02-02-SUMMARY.md
</output>
