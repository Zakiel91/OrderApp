---
phase: 05-order-detail-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
  - src/i18n/he.json
  - src/i18n/en.json
  - src/i18n/ru.json
autonomous: true
requirements:
  - DET-04

must_haves:
  truths:
    - "CSS-токены для iOS-палитры доступны во всём приложении через var(--token)"
    - "Анимация slideUp объявлена в глобальных стилях"
    - "Новые i18n-ключи (tab_client, tab_item, tab_payment, tab_notes, cancel, save_success) переведены на три языка"
  artifacts:
    - path: "src/index.css"
      provides: "iOS design tokens + slideUp keyframe"
      contains: "--color-ios-bg, --color-separator, --color-text-secondary, --radius-lg, --radius-full, --shadow-sm, --shadow-sheet, --status-new-bg, --status-new-text, --status-production-bg, --status-production-text, --status-done-bg, --status-done-text, slideUp keyframe"
    - path: "src/i18n/he.json"
      provides: "Hebrew translations for new keys"
      contains: "tab_client, tab_item, tab_payment, tab_notes, cancel, save_success, whatsapp_action, status_change_action"
    - path: "src/i18n/en.json"
      provides: "English translations for new keys"
      contains: "tab_client, tab_item, tab_payment, tab_notes, cancel, save_success"
    - path: "src/i18n/ru.json"
      provides: "Russian translations for new keys"
      contains: "tab_client, tab_item, tab_payment, tab_notes, cancel, save_success"
  key_links:
    - from: "src/pages/OrderDetailPage.tsx"
      to: "src/index.css"
      via: "var(--color-ios-bg) in className/style props"
      pattern: "var\\(--color-ios-bg\\)"
    - from: "src/components/FieldEditSheet.tsx"
      to: "src/index.css"
      via: "var(--shadow-sheet) in inline style"
      pattern: "var\\(--shadow-sheet\\)"
---

<objective>
Добавить iOS-токены дизайна и анимацию slideUp в глобальные стили, и добавить новые i18n-ключи для интерфейса вкладок и редактирования в три языковых файла.

Purpose: Все последующие компоненты фазы 5 зависят от этих токенов и ключей. Без них Plan 02 и Plan 03 не могут использовать правильные цвета, тени и переводы.
Output: Обновлённый src/index.css с iOS-токенами + slideUp, обновлённые he.json/en.json/ru.json с 8 новыми ключами.
</objective>

<execution_context>
@C:/Users/YoseFFF/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/YoseFFF/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/OrderApp/.planning/PROJECT.md
@C:/OrderApp/.planning/ROADMAP.md
@C:/OrderApp/.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Добавить iOS-токены и keyframe slideUp в src/index.css</name>
  <read_first>
    src/index.css — прочитать весь файл, чтобы понять существующие токены и не дублировать их; найти блок @keyframes (строки 113–127) для вставки slideUp рядом
  </read_first>
  <files>src/index.css</files>
  <action>
Добавить в конец блока `:root` в `src/index.css` (после строки `--color-error: #dc2626;` и до закрывающей `}`) следующие новые iOS-токены:

```css
  /* iOS design tokens — Phase 5 Order Detail */
  --color-ios-bg: #f2f2f7;
  --color-separator: rgba(60,60,67,0.18);
  --color-text-secondary: rgba(60,60,67,0.6);
  --radius-lg: 16px;
  --radius-full: 9999px;
  --shadow-sm: 0 1px 3px rgba(0,0,0,0.06), 0 1px 2px rgba(0,0,0,0.04);
  --shadow-sheet: 0 -2px 20px rgba(0,0,0,0.12);
  --status-new-bg: rgba(0,122,255,0.12);
  --status-new-text: #007aff;
  --status-production-bg: rgba(255,149,0,0.12);
  --status-production-text: #e08000;
  --status-done-bg: rgba(52,199,89,0.12);
  --status-done-text: #248a3d;
```

Затем добавить `@keyframes slideUp` сразу после существующего `@keyframes slideInRight` (после строки 127):

```css
@keyframes slideUp {
  from { transform: translateY(100%); }
  to   { transform: translateY(0); }
}
```

ВАЖНО: НЕ изменять существующие токены `--color-primary`, `--color-bg`, `--color-surface`, `--color-text`, `--color-text-muted`, `--color-border`, `--color-success`, `--color-warning`. Добавлять только новые переменные.
  </action>
  <verify>
    <automated>grep -c "color-ios-bg\|color-separator\|radius-lg\|radius-full\|shadow-sheet\|status-new-bg\|slideUp" /c/OrderApp/src/index.css</automated>
  </verify>
  <done>grep возвращает 7 (каждый токен/keyframe присутствует). Команда `grep "color-primary" /c/OrderApp/src/index.css` по-прежнему возвращает `--color-primary: #1a3a5c` (оригинальное значение не изменено).</done>
</task>

<task type="auto">
  <name>Task 2: Добавить новые i18n-ключи в he.json, en.json, ru.json</name>
  <read_first>
    src/i18n/he.json — прочитать последние 10 строк для понимания структуры и последнего ключа перед закрывающей `}`
    src/i18n/en.json — прочитать последние 10 строк
    src/i18n/ru.json — прочитать последние 10 строк
  </read_first>
  <files>src/i18n/he.json, src/i18n/en.json, src/i18n/ru.json</files>
  <action>
Добавить следующие ключи в каждый из трёх JSON-файлов, вставив их перед последней закрывающей `}` (после строки `"delete_error": "..."`).

Каждый файл сейчас заканчивается на `"delete_error": "..."` без запятой перед `}`. Добавить запятую после `delete_error` и добавить новые ключи:

**src/i18n/he.json** — добавить:
```json
  "tab_client": "לקוח",
  "tab_item": "פריט",
  "tab_payment": "תשלום",
  "tab_notes": "הערות",
  "cancel": "ביטול",
  "save_success": "השינוי נשמר",
  "whatsapp_action": "WhatsApp",
  "status_change_action": "סטטוס"
```

**src/i18n/en.json** — добавить:
```json
  "tab_client": "Client",
  "tab_item": "Item",
  "tab_payment": "Payment",
  "tab_notes": "Notes",
  "cancel": "Cancel",
  "save_success": "Change saved",
  "whatsapp_action": "WhatsApp",
  "status_change_action": "Status"
```

**src/i18n/ru.json** — добавить:
```json
  "tab_client": "Клиент",
  "tab_item": "Товар",
  "tab_payment": "Оплата",
  "tab_notes": "Примечания",
  "cancel": "Отмена",
  "save_success": "Изменение сохранено",
  "whatsapp_action": "WhatsApp",
  "status_change_action": "Статус"
```

ВАЖНО: убедиться что каждый файл остаётся валидным JSON (запятые между записями, нет trailing comma перед `}`).
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('/c/OrderApp/src/i18n/he.json','utf8')); JSON.parse(require('fs').readFileSync('/c/OrderApp/src/i18n/en.json','utf8')); JSON.parse(require('fs').readFileSync('/c/OrderApp/src/i18n/ru.json','utf8')); console.log('all valid')"</automated>
  </verify>
  <done>Команда node выводит "all valid". `grep "tab_client" /c/OrderApp/src/i18n/he.json` возвращает строку с `"לקוח"`. `grep "tab_client" /c/OrderApp/src/i18n/ru.json` возвращает строку с `"Клиент"`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS файл → браузер | Нет пользовательского ввода; только статичные токены |
| JSON файл → i18n контекст | Статичные строки перевода; нет исполняемого кода |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | src/index.css | accept | Изменения только в CSS-переменных; нет runtime-логики; проверяется grep-валидацией |
| T-05-01-02 | Tampering | i18n JSON files | accept | Статичные строки перевода; JSON-валидация через `node -e JSON.parse` в verify |
</threat_model>

<verification>
После выполнения обоих задач:
1. `grep "color-ios-bg" /c/OrderApp/src/index.css` — должен вернуть одну строку с `--color-ios-bg: #f2f2f7`
2. `grep "slideUp" /c/OrderApp/src/index.css` — должен вернуть объявление keyframe
3. `grep "tab_client" /c/OrderApp/src/i18n/he.json` — должен вернуть `"tab_client": "לקוח"`
4. Все три JSON-файла проходят валидацию через node
5. Существующий `--color-primary: #1a3a5c` не изменён
</verification>

<success_criteria>
- 13 новых CSS-переменных добавлены в :root без изменения существующих
- @keyframes slideUp присутствует в src/index.css
- 8 новых i18n-ключей присутствуют в каждом из трёх языковых файлов
- Все три JSON-файла валидны
</success_criteria>

<output>
После выполнения создать `.planning/phases/05-order-detail-redesign/05-01-SUMMARY.md`
</output>
