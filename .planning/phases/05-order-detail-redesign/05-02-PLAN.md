---
phase: 05-order-detail-redesign
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/components/FieldEditSheet.tsx
autonomous: true
requirements:
  - DET-03
  - DET-04

must_haves:
  truths:
    - "FieldEditSheet открывается снизу с iOS-анимацией при передаче пропсов"
    - "Шторка содержит drag handle, заголовок Cancel/FieldName/Save, underline-инпут"
    - "Нажатие Cancel или на оверлей закрывает шторку без сохранения"
    - "Нажатие Save вызывает onSave(field, value) и блокирует кнопку пока идёт запрос"
    - "Инпут получает фокус через 300мс после открытия (после анимации)"
    - "Шторка НЕ показывается когда заказ не в статусе 'new' — ответственность вызывающего компонента"
  artifacts:
    - path: "src/components/FieldEditSheet.tsx"
      provides: "Bottom sheet component for single-field editing"
      exports: ["FieldEditSheet"]
      contains: "slideUp animation, onSave prop, onClose prop, autoFocus after 300ms"
  key_links:
    - from: "src/pages/OrderDetailPage.tsx"
      to: "src/components/FieldEditSheet.tsx"
      via: "import { FieldEditSheet } from '../components/FieldEditSheet'"
      pattern: "FieldEditSheet"
    - from: "src/components/FieldEditSheet.tsx"
      to: "src/index.css"
      via: "var(--shadow-sheet) in inline style prop"
      pattern: "shadow-sheet"
---

<objective>
Создать переиспользуемый компонент `FieldEditSheet` — iOS-стилизованную шторку редактирования одного поля, которая скользит снизу вверх.

Purpose: Plan 03 использует этот компонент для каждого редактируемого поля в `OrderDetailPage`. Изолирование логики шторки в отдельный компонент упрощает тестирование и повторное использование.
Output: `src/components/FieldEditSheet.tsx` — готовый к использованию компонент с полной анимацией и логикой сохранения.
</objective>

<execution_context>
@C:/Users/YoseFFF/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/YoseFFF/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/OrderApp/.planning/PROJECT.md
@C:/OrderApp/.planning/ROADMAP.md
@C:/OrderApp/.planning/phases/05-order-detail-redesign/05-01-SUMMARY.md

<interfaces>
<!-- Контракты для исполнителя. Извлечено из кодовой базы. -->

Из src/lib/api.ts (строка 80):
```typescript
export async function updateOrder(data: Record<string, unknown>): Promise<{ success: boolean }>
// Вызывает PUT /api/production/orders с auto-injected Authorization header
// Числовые поля: price_to_client → parseFloat(value) перед передачей
```

Из src/context/LanguageContext.tsx (паттерн использования):
```typescript
const { t } = useLanguage()
// t('cancel') → 'ביטול' / 'Cancel' / 'Отмена'
// t('save_changes') → 'שמור שינויים' / 'Save Changes' / 'Сохранить изменения'
```

Из src/index.css (новые токены — добавлены в Plan 01):
```css
--shadow-sheet: 0 -2px 20px rgba(0,0,0,0.12);
--color-separator: rgba(60,60,67,0.18);
--color-text-secondary: rgba(60,60,67,0.6);
@keyframes slideUp { from { transform: translateY(100%); } to { transform: translateY(0); } }
```

Аналог — src/components/MetalWheelPicker.tsx (паттерн оверлея):
```typescript
// Использует fixed inset-0 z-[100] для оверлея
// Смешивает Tailwind classes + inline style={{ ... }} для iOS-специфичных свойств
// Нет CSS-модулей, нет styled-components
```

Аналог — src/components/DraftSavedToast.tsx (паттерн fixed overlay):
```typescript
// fixed top-4 left-1/2 -translate-x-1/2 z-40
// animate-[fadeIn_150ms_ease-out]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать src/components/FieldEditSheet.tsx</name>
  <read_first>
    src/components/MetalWheelPicker.tsx — изучить паттерн оверлея: как используется fixed positioning, inline style vs Tailwind, обработка stopPropagation
    src/components/DraftSavedToast.tsx — паттерн fixed overlay и animate-[...] синтаксис
    src/context/LanguageContext.tsx — сигнатура useLanguage hook
    src/index.css — убедиться что slideUp и --shadow-sheet добавлены в Plan 01 (проверить наличие)
  </read_first>
  <files>src/components/FieldEditSheet.tsx</files>
  <action>
Создать новый файл `src/components/FieldEditSheet.tsx` со следующей полной реализацией:

**Интерфейс пропсов:**
```typescript
interface FieldEditSheetProps {
  field: string        // DB column name, e.g. 'client_name_raw'
  label: string        // translated display label, e.g. t('client_name')
  value: string        // current field value
  type?: string        // input type: 'text' | 'tel' | 'email' | 'number' | 'date' (default: 'text')
  saving?: boolean     // true while PATCH is in flight — disables Save button
  onSave: (field: string, value: string) => Promise<void>
  onClose: () => void
}
```

**Логика компонента:**
- `localValue` — локальный state, инициализируется из `value` при монтировании
- `inputRef` — useRef для программного фокуса
- `useEffect` при монтировании: `setTimeout(() => inputRef.current?.focus(), 300)` — задержка 300мс позволяет завершиться анимации slideUp
- `handleSave`: вызывает `await onSave(field, localValue)` — ошибки обрабатывает вызывающий компонент

**JSX-структура:**
```tsx
// Оверлей — клик по фону закрывает без сохранения
<div
  className="fixed inset-0 z-[100] flex items-end"
  style={{ background: 'rgba(0,0,0,0.35)', animation: 'fadeIn 0.2s ease' }}
  onClick={onClose}
>
  {/* Шторка — stopPropagation чтобы клик внутри не закрывал */}
  <div
    className="w-full bg-[var(--color-surface)] pb-10"
    style={{
      borderRadius: '20px 20px 0 0',
      animation: 'slideUp 0.28s cubic-bezier(0.32,0.72,0,1)',
      boxShadow: 'var(--shadow-sheet)',
    }}
    onClick={e => e.stopPropagation()}
  >
    {/* Drag handle */}
    <div
      className="mx-auto mt-2.5"
      style={{ width: 36, height: 5, borderRadius: 3, background: 'rgba(60,60,67,0.18)' }}
    />

    {/* Заголовок: Cancel · FieldName · Save */}
    <div
      className="flex justify-between items-center px-5 py-3.5"
      style={{ borderBottom: '1px solid var(--color-separator)' }}
    >
      <button
        type="button"
        className="text-[var(--color-text-muted)] text-[17px] bg-transparent border-none cursor-pointer font-[var(--font-body)]"
        onClick={onClose}
      >
        {t('cancel')}
      </button>
      <span className="text-[17px] font-semibold text-[var(--color-text)]">{label}</span>
      <button
        type="button"
        className="text-[var(--color-primary)] text-[17px] font-semibold bg-transparent border-none cursor-pointer disabled:opacity-40 font-[var(--font-body)]"
        disabled={saving}
        onClick={handleSave}
      >
        {saving ? '…' : t('save_changes')}
      </button>
    </div>

    {/* Инпут */}
    <div className="px-5 pt-5 pb-2">
      <input
        ref={inputRef}
        type={type || 'text'}
        value={localValue}
        onChange={e => setLocalValue(e.target.value)}
        className="w-full text-[17px] bg-transparent outline-none pb-2 text-[var(--color-text)]"
        style={{
          border: 'none',
          borderBottom: '2px solid var(--color-primary)',
          textAlign: 'right',
        }}
      />
    </div>
  </div>
</div>
```

**Импорты в начале файла:**
```typescript
import { useEffect, useRef, useState } from 'react'
import { useLanguage } from '../context/LanguageContext'
```

**Экспорт:**
```typescript
export { FieldEditSheet }
// или: export function FieldEditSheet(...) { ... }
```

RTL-примечание: `textAlign: 'right'` в inline style (не Tailwind), потому что Tailwind `text-right` не применяется внутри RTL контекста предсказуемо на iOS.
  </action>
  <verify>
    <automated>grep -c "FieldEditSheet\|slideUp\|stopPropagation\|setTimeout.*300\|onSave\|onClose" /c/OrderApp/src/components/FieldEditSheet.tsx</automated>
  </verify>
  <done>
    - Файл `src/components/FieldEditSheet.tsx` существует
    - grep возвращает 6 (все ключевые паттерны присутствуют)
    - `grep "export" /c/OrderApp/src/components/FieldEditSheet.tsx` показывает `export function FieldEditSheet` или `export { FieldEditSheet }`
    - `grep "useLanguage" /c/OrderApp/src/components/FieldEditSheet.tsx` возвращает строку с импортом
    - `grep "textAlign.*right\|text-align.*right" /c/OrderApp/src/components/FieldEditSheet.tsx` возвращает строку (RTL-стиль применён)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Пользовательский ввод → onSave | Значение из input передаётся в onSave; валидация — ответственность вызывающего компонента и сервера |
| onSave → PUT /api/production/orders | updateOrder в api.ts; сервер проверяет ownership и status !== 'new' (403) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering | FieldEditSheet input → onSave | mitigate | Вызывающий компонент (OrderDetailPage) показывает шторку только при status === 'new'; сервер возвращает 403 при попытке изменить non-new заказ |
| T-05-02-02 | Denial of Service | Save button double-tap | mitigate | `disabled={saving}` блокирует повторный вызов пока предыдущий запрос в полёте |
| T-05-02-03 | Information Disclosure | Overlay z-index | accept | z-[100] перекрывает всё содержимое страницы; это ожидаемое поведение модального компонента |
</threat_model>

<verification>
После выполнения:
1. `ls /c/OrderApp/src/components/FieldEditSheet.tsx` — файл существует
2. `grep "export.*FieldEditSheet" /c/OrderApp/src/components/FieldEditSheet.tsx` — экспорт присутствует
3. `grep "slideUp.*0.28s.*cubic-bezier" /c/OrderApp/src/components/FieldEditSheet.tsx` — анимация корректная
4. `grep "setTimeout.*300" /c/OrderApp/src/components/FieldEditSheet.tsx` — задержка фокуса присутствует
5. `grep "stopPropagation" /c/OrderApp/src/components/FieldEditSheet.tsx` — предотвращение закрытия при клике внутри
</verification>

<success_criteria>
- Компонент FieldEditSheet создан и экспортирован
- Оверлей с backdrop rgba(0,0,0,0.35) + анимация fadeIn
- Шторка с borderRadius 20px 20px 0 0 + анимация slideUp 0.28s cubic-bezier(0.32,0.72,0,1)
- Drag handle отрисован (36×5px, rgba(60,60,67,0.18))
- Заголовок: Cancel (t('cancel')) / label / Save (t('save_changes'))
- Save отключён (disabled) когда saving=true
- Фокус на инпуте через 300мс setTimeout
- textAlign: 'right' для RTL
- Клик по оверлею вызывает onClose; клик внутри шторки — stopPropagation
</success_criteria>

<output>
После выполнения создать `.planning/phases/05-order-detail-redesign/05-02-SUMMARY.md`
</output>
