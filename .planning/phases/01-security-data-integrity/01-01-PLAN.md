---
phase: 01-security-data-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/LoginPage.tsx
  - src/context/AuthContext.tsx
autonomous: true
requirements:
  - BUG-04

must_haves:
  truths:
    - "PIN 9119 cannot be entered on the login screen — the dev input field does not exist"
    - "The string '9119' does not appear in the compiled production bundle"
    - "loginWithDevCode does not appear in the AuthContextType interface, AuthProvider implementation, or context value"
    - "IS_DEV and DEV_CODE constants are not present anywhere in LoginPage.tsx"
    - "Google One Tap is the only login path — it triggers unconditionally on mount"
  artifacts:
    - path: "src/pages/LoginPage.tsx"
      provides: "Login page with Google-only auth, dev block fully removed"
    - path: "src/context/AuthContext.tsx"
      provides: "AuthContext with loginWithDevCode removed from interface, implementation, and provider value"
  key_links:
    - from: "src/pages/LoginPage.tsx"
      to: "src/context/AuthContext.tsx"
      via: "useAuth() destructuring"
      pattern: "loginWithGoogle.*isLoading.*authError"
---

<objective>
Remove all development backdoor code from the production authentication path.

Purpose: A hardcoded PIN (9119) in LoginPage.tsx and a matching loginWithDevCode callback in AuthContext.tsx allow anyone on localhost to bypass Google authentication and receive admin-level access. This code ships to production in the compiled bundle where it is visible in the source map. It must be deleted entirely — not feature-flagged, not commented out.

Output:
- LoginPage.tsx with only the Google One Tap UI (dev input block gone)
- AuthContext.tsx with loginWithDevCode removed from the interface, the useCallback implementation, and the provider value prop
</objective>

<execution_context>
@C:/Users/YoseFFF/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/YoseFFF/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/OrderApp/.planning/PROJECT.md
@C:/OrderApp/.planning/ROADMAP.md
@C:/OrderApp/.planning/STATE.md
@C:/OrderApp/.planning/phases/01-security-data-integrity/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dev backdoor from LoginPage.tsx</name>
  <files>src/pages/LoginPage.tsx</files>

  <read_first>
    - src/pages/LoginPage.tsx — read the full file before touching anything; understand what survives the deletion
  </read_first>

  <action>
Delete the following from src/pages/LoginPage.tsx (per D-01, D-02):

1. Line 6: `const IS_DEV = window.location.hostname === 'localhost' || window.location.hostname === '127.0.0.1'`
   Delete this entire line.

2. Line 7: `const DEV_CODE = '9119'`
   Delete this entire line.

3. The `useAuth()` destructure on line 10 currently reads:
   `const { loginWithGoogle, loginWithDevCode, isLoading, authError } = useAuth()`
   Change it to:
   `const { loginWithGoogle, isLoading, authError } = useAuth()`

4. Lines 13-14: the two useState declarations for devInput and devError:
   `const [devInput, setDevInput] = useState('')`
   `const [devError, setDevError] = useState('')`
   Delete both lines.

5. The useEffect (lines 16-21) currently has an `if (!IS_DEV)` guard:
   ```
   useEffect(() => {
     if (!IS_DEV) {
       const timer = setTimeout(() => loginWithGoogle(), 500)
       return () => clearTimeout(timer)
     }
   }, [loginWithGoogle])
   ```
   Remove the `if (!IS_DEV)` guard so Google One Tap fires unconditionally:
   ```
   useEffect(() => {
     const timer = setTimeout(() => loginWithGoogle(), 500)
     return () => clearTimeout(timer)
   }, [loginWithGoogle])
   ```

6. Lines 23-30: the entire `handleDevLogin` function:
   ```
   const handleDevLogin = () => {
     if (devInput === DEV_CODE) {
       loginWithDevCode()
     } else {
       setDevError('Wrong code')
       setTimeout(() => setDevError(''), 2000)
     }
   }
   ```
   Delete the entire function.

7. Inside the JSX return, the entire `{IS_DEV && (...)}` block (lines 40-67):
   ```
   {/* Dev login — localhost only */}
   {IS_DEV && (
     <div className="space-y-2">
       ...
     </div>
   )}
   ```
   Delete this block including the comment above it.

After deletions the file must compile without TypeScript errors. No other lines should be changed.
  </action>

  <verify>
    <automated>cd /c/OrderApp && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "IS_DEV\|DEV_CODE\|9119\|loginWithDevCode\|devInput\|devError\|handleDevLogin" src/pages/LoginPage.tsx` returns no output
    - `grep -n "loginWithGoogle" src/pages/LoginPage.tsx` returns at least 2 lines (destructure + onClick usage)
    - `grep -A3 "useEffect" src/pages/LoginPage.tsx` shows `const timer = setTimeout` as the first line inside the effect — no `if (!IS_DEV)` guard
    - `npx tsc --noEmit` exits 0 (or only pre-existing errors unrelated to this file)
  </acceptance_criteria>

  <done>LoginPage.tsx contains no IS_DEV, DEV_CODE, 9119, loginWithDevCode, devInput, devError, or handleDevLogin. Google One Tap fires unconditionally on mount.</done>
</task>

<task type="auto">
  <name>Task 2: Remove loginWithDevCode from AuthContext.tsx</name>
  <files>src/context/AuthContext.tsx</files>

  <read_first>
    - src/context/AuthContext.tsx — read the full file; note the interface (lines 15-23), the loginWithDevCode useCallback (lines 117-127), and the provider value prop (lines 141-145)
  </read_first>

  <action>
Delete the following from src/context/AuthContext.tsx (per D-01, D-04):

1. In the `AuthContextType` interface (around line 18), delete this line:
   `loginWithDevCode: () => void`
   The interface should retain: user, loginWithGoogle, logout, isAdmin, isLoading, authError.

2. Delete the entire `loginWithDevCode` useCallback implementation (lines 117-127):
   ```
   const loginWithDevCode = useCallback(() => {
     const devUser: User = {
       email: 'admin@innovationdia.com',
       name: 'Dev Admin',
       picture: '',
       prefix: 'INNO',
       role: 'admin',
     }
     setUser(devUser)
     localStorage.setItem('user', JSON.stringify(devUser))
   }, [])
   ```
   Delete the entire block.

3. In the `AuthContext.Provider` value prop (around line 142), remove `loginWithDevCode` from the object:
   Current:
   `user, loginWithGoogle, loginWithDevCode, logout,`
   Change to:
   `user, loginWithGoogle, logout,`

After deletions the file must compile without TypeScript errors and the context must still export all remaining fields correctly.
  </action>

  <verify>
    <automated>cd /c/OrderApp && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "loginWithDevCode\|Dev Admin\|admin@innovationdia\|9119" src/context/AuthContext.tsx` returns no output
    - `grep -n "loginWithDevCode" src/context/AuthContextType` — interface block no longer contains the method
    - `grep -c "loginWithGoogle" src/context/AuthContext.tsx` returns at least 2 (still present in interface and implementation)
    - `npx tsc --noEmit` exits 0 (or only pre-existing errors unrelated to this file)
  </acceptance_criteria>

  <done>AuthContext.tsx interface, implementation, and provider value contain no loginWithDevCode. The context still exports loginWithGoogle, logout, user, isAdmin, isLoading, authError.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localhost → App | Dev-only code path that bypasses Google auth entirely |
| Bundle → Client | Compiled JS shipped to production contains hardcoded admin credentials and PIN |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | LoginPage.tsx / loginWithDevCode | mitigate | Delete loginWithDevCode and all call sites; admin access only via verified Google credential flow |
| T-01-02 | Elevation of Privilege | AuthContext.tsx / loginWithDevCode | mitigate | Remove from AuthContextType interface and AuthProvider value; no surface remains for caller to invoke |
| T-01-03 | Information Disclosure | LoginPage.tsx / DEV_CODE constant | mitigate | Delete DEV_CODE = '9119' constant; string will not appear in compiled bundle |
</threat_model>

<verification>
After both tasks complete:

1. `grep -rn "loginWithDevCode\|DEV_CODE\|IS_DEV\|9119\|handleDevLogin\|devInput\|devError" src/` returns no matches
2. `npx tsc --noEmit` exits clean
3. `npm run build` succeeds; `grep -r "9119" dist/` returns no matches
</verification>

<success_criteria>
- The string "9119" does not appear anywhere in src/ or dist/
- loginWithDevCode does not appear in any .ts or .tsx file under src/
- The compiled bundle passes a text search for "9119" with zero hits
- TypeScript compilation is error-free for the modified files
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-data-integrity/01-01-SUMMARY.md` using the summary template.
</output>
